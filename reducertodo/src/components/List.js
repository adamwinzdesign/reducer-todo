import React from 'react';
import ListItem from './ListItem';

// not getting any props??

const List = props => (
  // console.log(props)
  // <div>
  //   <div>{props.list.map((element, index) => {
  //     return <ListItem key={index} index={index} toDo={element} />;
  //   })};
  //   </div>
  // </div>

  <div>
    {/* <div>{props.list.map((element, index) => {
      return <List key={index} index={index} toDo={element} />;
    })}
    </div> */}
    <h1>test</h1>
  </div>
)

export default List;